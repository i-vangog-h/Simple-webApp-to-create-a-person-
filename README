- to open a database in console: 
psql -d 'postgresql://postgres:mysecretpassword@127.0.0.1:5432/postgres'

- to start docker:
    sudo systemctl start docker
    (docker ps) //shows whats inside
    docker rm ' ...container... '                                                                                      (deletes an old container)
    sudo docker run --name some-postgres -e POSTGRES_PASSWORD=mysecretpassword -d -p 5432:5432 postgres                       (creates a new one)



* history | grep '...' (shows where '...' was used)